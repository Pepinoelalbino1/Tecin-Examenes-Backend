-- Script de creación de esquema (opcional, JPA puede crear las tablas automáticamente)

CREATE DATABASE IF NOT EXISTS mina_examenes;
USE mina_examenes;

-- La creación de tablas se realiza automáticamente por JPA con ddl-auto=update
-- Este script es solo de referencia
